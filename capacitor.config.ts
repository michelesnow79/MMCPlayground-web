import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.missmeconnection.app',
  appName: 'Miss Me Connection',
  webDir: 'dist'
};

export default config;
